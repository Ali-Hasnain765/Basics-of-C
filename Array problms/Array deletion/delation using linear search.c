
#include <stdio.h>
int main()
{
   int arr[100], i, n,src,newindex;

   printf("Enter number of elements in array\n");
   scanf("%d", &n);

printf("Enter the search : ");
    scanf("%d",&src);
    for (i=0;i<n;i++)
    {
    scanf("%d,&a[i]")
for(i=o;i<n;i++)
{
    printf(" %d",a[i]);
}
for(i=0;i<n;i++)
{
    if (a[i]==search)
}
